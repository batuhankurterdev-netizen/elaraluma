# Copy this file to 'android/key.properties' and fill the values.
# Do NOT commit the real key.properties to version control.

# Path to the keystore file (relative to the android/ folder), example:
# storeFile=key.jks
storeFile=

# Passwords and alias for your signing key
# storePassword=your_store_password
storePassword=
# keyPassword=your_key_password
keyPassword=
# keyAlias=your_key_alias
keyAlias=
